# Mini-UNA — снимок состояния, 2026-09-23

Дополнение к `ANALYSIS_REPORT.md` (тот анализировал `58f704f`). Здесь — что изменилось и что проверено на свежем дереве.

## База снимка

- HEAD: `a0424ce` + 12 незакоммиченных файлов (пачка ниже)
- Инструментов: **41** (`needle_tools.json` валиден); все 41 покрыты `ToolRegistry` (проверено grep)
- Kotlin: баланс скобок во всех файлах; `AndroidManifest.xml` валиден
- APK в папке: ран 108 от 08:15 (до текущей пачки)

## Что добавлено после отчёта

- Мультишаг: `classifyAll` (fastPath → compound-сплит → native multi) + параллельное исполнение (пул 3, системная навигация — по порядку)
- Слоты и опрос: `PendingSlot`, `SlotHelper` (time/days/duration/delay/dismiss_time/app/reply_text), голосовой переспрос + диалог-фолбэк, «отмена»
- Голосовое подтверждение вместо диалога при низкой уверенности (`pendingConfirm`, да/нет)
- Сброс сессии Needle (`POST /reset`) после каждого ответа — убраны галлюцинации из истории
- Пол уверенности 0.25 (ниже — «не понял») + дедуп одинаковых вызовов
- Needle serve: официальный бинарь в APK (`assets/needle` + `libneedle.so`), автозапуск на localhost:18123, dual-stack, лог в ошибке, `usesCleartextTraffic`, настройки глубины/контекста/порога + рестарт
- Уведомления: чтение, избранное с авто-озвучкой, **новые контакты**, **ответы через кнопку RemoteInput** (`NOTIF_REPLY`)
- Экран: чтение + **тапы по тексту** (`SCREEN_TAP`)
- On/off: `MEDIA_PAUSE`, `WIFI_PANEL`/`BT_PANEL` (системные панели), `ALARM_DISMISS` (время + snooze, слот «во сколько»), `TIMER_CANCEL` (`ACTION_DISMISS_TIMER`)
- Музыка: мост NeonWave (`AUTOPLAY` + плейлист, автовозврат), NeonWave — плеер по умолчанию
- Персона, хотворд «Юна» (foreground-сервис), кэш голосовых фраз, темп речи, иконка, `SET_TIMER`-заглушка, `extractNativeLibs`
- `PLAN.md` — фазы 1–2, 6; этот файл — для GitHub

## Открыто (по приоритету)

1. Валидация serve и моста на устройстве (нужны свежие APK обеих сторон)
2. P0-1 из отчёта: стрип `.so` (APK 51 МБ → ~45 и меньше)
3. P1-6: слияние двух каталогов приложений; P1-11: офлайн-ASR; P1-12: wrapper + тесты
4. Точные будильники (`SCHEDULE_EXACT_ALARM`), периодические вотчеры, быстрые ответы на уведомления
5. Upstream `cactus#813`: бандл needle3 (молчание)

## Как проверить эту пачку (устройство)

1. «выключи музыку» → «Музыка остановлена»; «включи вайфай» → панель
2. «выключи будильник» → вопрос «во сколько?» → выключен; «отложи на 10 минут» → snooze
3. «отмени таймер» → «Выключаю таймер»
4. «ответь маме иду» (по уведомлению с кнопкой) → «Ответила»
5. «нажми на …» на открытом экране → «Нажала …»
6. Статус модели: сервер + время ответа; страница лога — причина, если нет
